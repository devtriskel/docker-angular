language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t devtriskel/docker-angular -f Dockerfile.dev .

script:
  - docker run -e CI=true devtriskel/docker-angular npm run test